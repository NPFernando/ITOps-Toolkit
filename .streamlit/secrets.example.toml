# Copy this file to `.streamlit/secrets.toml` for local-only secrets.
# Never commit `.streamlit/secrets.toml`.

# Optional, reserved for future direct OpenAI support. This app does not use
# direct OpenAI API calls.
OPENAI_API_KEY = ""

# Optional Azure AI Foundry / Azure OpenAI settings for Log Troubleshooting
# Assistant summaries. Logs are sent only when configured and explicitly
# enabled per submission.
# Use the endpoint and deployment from your Azure AI Foundry model deployment.
AZURE_OPENAI_API_KEY = ""
AZURE_OPENAI_ENDPOINT = ""
AZURE_OPENAI_DEPLOYMENT = ""
AZURE_OPENAI_API_VERSION = ""
